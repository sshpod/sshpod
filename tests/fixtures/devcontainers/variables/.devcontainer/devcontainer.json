{
  "image": "${localEnv:DEVCONTAINER_IMAGE:alpine:latest}",
  "workspaceFolder": "${containerWorkspaceFolder}",
  "containerEnv": {
    "HOME_FROM_HOST": "${localEnv:HOME}",
    "ROOT": "${localWorkspaceFolder}"
  }
}
